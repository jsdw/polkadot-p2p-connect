# Warp sync example

This example makes repeated requests to a node to obtain warp sync information, varifying it as it goes until we are up to date.

Use `cargo run` in this folder to run the example.